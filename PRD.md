# 产品需求文档 (PRD)：高效番茄钟网站——PomoNow

## 1. 产品概述和目标用户

### 1.1 产品愿景

打造一款极简、优雅且高效的网页版番茄钟工具。通过消除视觉干扰、整合任务管理与数据可视化，帮助用户进入深度工作状态（Flow），培养良好的时间管理习惯。

### 1.2 目标用户群体

- **学生与备考族：** 需要长时间集中注意力复习，对阶段性休息和学习时长统计有强需求。
- **远程办公者/自由职业者：** 缺乏办公室氛围约束，需要工具来划分工作与休息边界，防止过度疲劳。
- **程序员/设计师/文字工作者：** 沉浸式创作者，需要“无打扰”的环境，同时希望记录各项目的实际耗时。

## 2. 详细的功能列表

### 2.1 核心计时模块（Timer）

- **标准番茄时钟：** 默认支持 25分钟专注、5分钟短休、15分钟长休（每完成4个番茄钟触发一次长休）。
- **计时控制：** 包含开始、暂停、跳过（Skip）、重置（Reset）功能。
- **自定义时长：** 用户可自由调整专注、短休、长休的时间长度。
- **状态自动流转：** 专注结束自动切换到休息，休息结束自动切换到专注（可配置是否自动开始）。

### 2.2 任务管理模块（Todo List）

- **任务看板：** 用户可创建、编辑、删除任务，并标记任务为“已完成”。
- **番茄数预测与绑定：** 创建任务时可预估所需的番茄数（如：写报告 预估 3 个番茄），并在专注时绑定当前任务，计时结束自动累加该任务的实际番茄数。

### 2.3 通知与音效（Alerts & Sounds）

- **提示音选择：** 专注/休息结束时播放提示音（支持多种音效：清脆铃声、数码滴滴、木鱼声等）。
- **背景白噪音：** 专注期间可选择播放白噪音（雨声、篝火、咖啡厅、Lo-Fi 音乐），支持音量调节。
- **浏览器通知：** 当页面处于后台标签页时，时间结束触发浏览器原生桌面通知。

### 2.4 数据统计模块（Analytics）

- **每日看板：** 今日完成番茄数、总专注时长、任务完成率。
- **历史趋势图：** 周/月/年维度的柱状图或折线图，展示时间分配和专注趋势。

### 2.5 用户系统与云同步（Account）

- **快捷登录：** 支持邮箱注册登录，以及第三方快捷登录（如 GitHub、Google、微信）。
- **多端同步：** 登录后数据自动同步至云端，更换电脑或浏览器数据不丢失。

## 3. 功能优先级（MVP 与后续版本）

为了快速验证市场并降低初始开发成本，建议采用迭代开发模式。

| **功能模块** | **MVP (最小可行性产品 - P0)**                  | **V1.1 (体验优化 - P1)**     | **V2.0 (高级进阶 - P2)**                         |
| ------------ | ---------------------------------------------- | ---------------------------- | ------------------------------------------------ |
| **计时器**   | 基础计时、开始/暂停/重置、标准25/5/15流转      | 自定义时长、状态自动流转开关 | 严格模式（倒计时期间禁止暂停）                   |
| **任务管理** | 本地简单 Todo（刷新即消失或存在 LocalStorage） | 任务与番茄钟绑定、预估番茄数 | 任务分类标签/清单（Projects）                    |
| **声音通知** | 1种结束提示音                                  | 多种提示音、网页后台桌面通知 | 丰富的白噪音混合器（混合雨声+咖啡厅）            |
| **数据统计** | 无（或仅限当日计数）                           | 本地存储的周/月基础图表      | 云端数据分析、专注热力图（类似GitHub Commit图）  |
| **用户系统** | 免登录，数据存本地                             | 注册/登录、云端数据同步      | 团队/房间协同专注（多人自习室）                  |
| **个性化**   | 基础明亮/暗黑模式                              | 自定义主题色                 | 更换精美动态背景（如 Pixel Art、Live Wallpaper） |

## 4. 界面设计要求 (UI/UX)

> **设计核心原则：** 极简主义、大字重、无干扰、沉浸感。

### 4.1 视觉风格

- **色彩心理学：** * **专注状态：** 采用沉浸、柔和的色调（如：番茄红 `#E74C3C` 或 深邃蓝 `#2C3E50`）。
  - **休息状态：** 切换为放松、舒缓的色调（如：清新绿 `#2ECC71` 或 柔和黄 `#F1C40F`）。
- **暗黑模式（Dark Mode）：** 必须支持，并能根据系统时间自动切换，保护深夜工作者的视力。

### 4.2 布局结构（推荐单页应用 SPA 布局）

- **核心区（中央）：** 巨大的倒计时数字（如 `25:00`），下方为明显的“开始/暂停”按钮。
- **顶部/侧边栏：** 极简导航，包含【番茄钟】、【任务清单】、【数据统计】、【设置】四个入口。
- **标签页标题动态更新：** 浏览器的 Tab 标题需实时显示剩余时间（例如：`(24:15) 专注中 - 番茄钟`），方便用户在浏览其他网页时瞟一眼进度。

## 5. 技术栈建议

基于当前的前端工程化趋势，推荐一套高效、轻量且扩展性强的技术栈：

### 5.1 前端 (Frontend)

- **框架：** **Next.js (React)** 或 **Vite + Vue 3**。如果是个人项目或追求极致性能，Vite + Vue3/React 极其轻量；如果后续考虑 SEO 和多页面扩展，Next.js 是绝佳选择。
- **样式：** **Tailwind CSS**。能够极快地实现响应式设计和暗黑模式。
- **状态管理：** **Zustand** (React) 或 **Pinia** (Vue)。用来管理计时器状态和全局设置。

### 5.2 后端与数据库 (Backend & Database - V1.1 引入)

- **全栈/无服务器方案：** **Supabase** 或 **Firebase**。
  - *强烈推荐 Supabase：* 它提供开箱即用的用户认证（Auth）、PostgreSQL 数据库以及实时同步功能，能让您在几小时内搞定原本需要写几周的后端逻辑。
- **自研传统后端（备选）：** Node.js (NestJS) + MongoDB/PostgreSQL。

### 5.3 部署与托管

- **前端托管：** Vercel 或 Netlify（全球 CDN 加速，全自动部署，且对个人免费）。

## 6. 非功能性需求

### 6.1 性能需求 (Performance)

- **首屏加载：** 页面首次加载时间（LCP）控制在 1.5 秒以内。
- **计时精准度（关键）：** > **技术痛点提示：** 浏览器的 `setInterval` 在标签页切到后台或电脑休眠时会被浏览器严重限流（导致计时变慢）。
  - **解决方案：** 必须使用 **Web Workers** 处理后台计时，或者在每次页面重新可见（`visibilitychange` 事件）时，通过对比系统当前时间戳（`Date.now()`）来校准剩余时间。

### 6.2 安全需求 (Security)

- **数据安全：** 用户密码必须经过加密存储（如 bcrypt），传输必须全站启用 HTTPS。
- **防刷限流：** API 接口需做 Rate Limiting，防止恶意刷取短信验证码或频繁请求接口。

### 6.3 易用性与无障碍 (Accessibility)

- **快捷键支持：** * `Space（空格键）`：开始 / 暂停计时。
  - `Esc`：重置当前计时。
- **响应式布局：** 完美适配 Mobile 端网页，确保用户用手机浏览器打开也能舒适使用。

如果您准备开始做这款产品，您倾向于**先做纯前端的 MVP 版本（数据存浏览器本地）**，还是**一开始就接入用户系统实现多端同步**呢？我们可以针对您的选择细化第一步的开发计划。